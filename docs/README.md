This folder is for documentation assets.

Place a screenshot named `SalaryLens.png` here so the README can display it:

- Path: `docs/SalaryLens.png`
- Suggested size: ~1200×700
- Keep the file reasonably small (<1 MB) for fast README rendering.
